DISARMED

This file is the only push-based trigger allowed for the Gate C production frontend deployment.
It must be deliberately changed so the first line is exactly:
DEPLOY GATE C FRONTEND
and include a non-empty request_id line.
